/*
  static void addProduct(Product prod) throws SQLException {
    Statement statement = connection.createStatement();

    String query = "INSERT INTO product(Name, UnitPrice) VALUES(, UnitPrice)";

    statement.executeQuery(query);
  }

  static void addRecord(Record record) throws SQLException {
    Statement statement = connection.createStatement();

    String query = "INSERT INTO record(Name, UnitPrice) VALUES('', UnitPrice)";

    statement.executeQuery(query);
  }

}
*/